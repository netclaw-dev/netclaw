# Netclaw.App

Minimal ASP.NET Core host for Netclaw runtime wiring (Slack adapter, ACL,
provider configuration, MCP integration, and persistence).

Current scaffold endpoints:

- `GET /`
- `GET /health/live`
- `GET /health/ready`
- `GET /api/runtime/info`
